# Replace evl_your_scorecard with an eval ID from `dari eval list` before creating.
name: Example Eval Router
enabled_models:
  - openai/gpt-5.6-sol
  - anthropic/claude-sonnet-5
provider_key_sources:
  openai: managed
  anthropic: managed
routing_strategy: slm
eval_ids:
  - evl_your_scorecard
